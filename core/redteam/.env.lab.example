# .env.lab :: Credenciales del Laboratorio Defensivo
# NUNCA subir este archivo a Git.
# Generado manualmente antes del primer 'docker compose up'.
# Ejemplo: DVWA_MYSQL_PASS=$(openssl rand -hex 16)

DVWA_MYSQL_PASS=CAMBIAR_ANTES_DE_USAR
